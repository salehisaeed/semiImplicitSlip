type        fieldMinMax;
libs        (fieldFunctionObjects);

mode        magnitude;
fields
(
    cellVolume
);

// Optional entries (runtime modifiable)
location    false;

// Optional (inherited) entries
writePrecision  12;
writeToFile     true;
enabled         true;
log             false;
